namespace Utilities
{
    public enum EntityType{
        None = 0,
        Hero,
        Enemy
    }

    public enum DataType
    {
        None = 0,
        Float,
        Int,
        Percentage
    }
}