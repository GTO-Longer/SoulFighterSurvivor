namespace Utilities
{
    public enum EntityType{
        None = 0,
        Hero,
        Enemy
    }
    
    public enum UIType{
        None = 0,
        Text,
        Button,
        Image,
        UINode
    }
}